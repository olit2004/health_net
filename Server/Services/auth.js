
import prisma from "../lib/prisma.js"





export const loginService = async (email, password) => {
    // Validate credentials, generate JWT tokens, return user data



}



export const registerService = async (userData) => {
    // Validate input, hash password, create user in database, return user info
};

